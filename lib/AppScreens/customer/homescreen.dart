import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Customer : HomeScreen', style: TextStyle(
            fontSize: 20,
            color: Colors.black,
        ),),
      ),
      
    );
  }
}