import 'package:flutter/material.dart';

class Categories extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Customer : Categories', style: TextStyle(
            fontSize: 20,
            color: Colors.black,
        ),),
      ),
      
    );
  }
}